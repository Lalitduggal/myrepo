dependency
- install python3



install


configure

inventory is searched in the following order:

/etc/ansible/
home directory
pwd


ansible --version

to test ansible
ansible -m ping


in case you have a different inventory file:

ansible-playbook 

